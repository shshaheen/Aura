import 'package:flutter/material.dart';

class FakeCall extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Make Fake Call")
      ),
    );
  }
}
